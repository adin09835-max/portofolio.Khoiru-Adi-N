# Environment & credentials — never commit these
.env
*.env
firebase-key.json
serviceAccountKey.json
*.pem
*.key

# Dependencies / build artifacts (not used by this static site, kept as a safeguard)
node_modules/
dist/
build/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/settings.json
